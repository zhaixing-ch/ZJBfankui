# RM 靶车项目文档

> 4×压敏(称重)传感器 → HX711 → STM32F103C8T6 ×4 → 泰山派 RK3566 → WebSocket → 网页控制台
>
> 更新日期：2026-09-10

---

## 1. 系统总体架构

```
┌─────────────┐   4路模拟    ┌────────┐  并行时序  ┌──────────────────┐
│ 压敏传感器×4 │ ──────────→ │ HX711×4 │ ────────→ │ STM32F103C8T6     │
│ (装甲板四角) │  毫伏级差分  │  A通道  │  DT/SCK   │ (每块装甲板1个)   │
└─────────────┘             └────────┘           └────────┬─────────┘
                                                          │ USART 115200-8-N-1
                                                          ▼
                                              ┌──────────────────────┐
                                              │ 泰山派 RK3566         │
                                              │ 串口聚合 + WebSocket  │
                                              │ + 静态托管前端页面    │
                                              └──────────┬───────────┘
                                                         │ ws://IP:8080 (JSON)
                                                         ▼
                                              ┌──────────────────────┐
                                              │ 浏览器控制台          │
                                              │ target_console/index.html │
                                              └──────────────────────┘
```

- 一个靶车共 **4 块装甲板**，每块装甲板 4 个压敏传感器 + 4 个 HX711 + 1 块 STM32。
- 每块装甲板可**独立设置 ID**（0~255），前端按 ID 单独显示击打位置与次数。
- 前端可切换**红/蓝方**（页面主题与灯条颜色联动）、**重置击打数**（单板/全部）。
- 靶车为**限轨**移动：前端控制前进/后退/停止 + 速度，另有旋转台开关、方向（顺/逆时针）与转速。

---

## 2. 硬件接线

### 2.1 称重传感器 → HX711（四线制，接 A 通道）

| 传感器线色 | 信号 | 接 HX711 |
|---|---|---|
| 红 | E+（激励正） | E+ |
| 黑 | E-（激励负） | E- |
| 白 | S+（信号正） | A+ |
| 灰 | S-（信号负） | A- |

- HX711 模块 VCC → 5V，GND 与 STM32 **共地**。
- 双通道 HX711 模块（E+E-A-A+B-B+）：传感器只用 **A 通道**，B 通道（B+/B-）**悬空**即可。
- 万用表可校验：四线传感器任意两根间有阻值（激励端约 1kΩ 左右、信号端更小），据此区分 E/S 线。

### 2.2 HX711 → STM32（PB 口，刻意避开 PB3/PB4 等 JTAG 复用脚，无需 remap）

| 通道 | DT(数据) | SCK(时钟) |
|---|---|---|
| CH1（左上） | PB0 | PB1 |
| CH2（右上） | PB5 | PB6 |
| CH3（右下） | PB7 | PB8 |
| CH4（左下） | PB9 | PB10 |

- USART1：PA9(TX) → USB转TTL 的 RX，**115200-8-N-1**，共地。
- 时钟：HSE 8MHz ×9 = 72MHz。

### 2.3 传感器安装位置约定

四个压敏传感器装在**官方小装甲板的四条 45° 切角边**附近：

```
CH1 左上 ─┐         ┌─ CH2 右上
          └  装甲板  ┘
          ┌          ┐
CH4 左下 ─┘         └─ CH3 右下
```

---

## 3. STM32 固件（`HX711_4ch_Scale/`）

### 3.1 文件

| 文件 | 说明 |
|---|---|
| `hx711.h` / `hx711.c` | 4 通道 HX711 驱动，`HX711_ReadAll()` 一次读 4 通道，超时返回 `HX711_ERR_VAL` |
| `main.c` | 主循环：采样滤波 → 去皮/标定 → 500ms 串口输出 |

### 3.2 关键参数（main.c 顶部可调）

| 宏 | 默认值 | 说明 |
|---|---|---|
| `SAMPLE_N` | 5 | 每通道滤波采样数（HX711 10SPS 时约 0.5s 出一组数） |
| `CAL_WEIGHT_G` | 1000.0 | 标定砝码克数 |
| `CAL_FACTOR_DEFAULT` | 8000.0 | 标定系数（4 通道 raw 之和 / 1g） |

### 3.3 使用流程（串口命令）

1. 秤台**空载**，发 `t` → 去皮（记录零点 `g_tare_raw`）
2. 放上已知重量砝码，发 `c` → 标定，串口打印系数
3. 把打印的系数填入 `CAL_FACTOR_DEFAULT`，下次上电免标定

### 3.4 数据处理

- **排序取中段平均**：每组 5 个采样去掉最大、最小后取平均，抗脉冲干扰。
- 500ms 周期输出：`CH1:xxx CH2:xxx CH3:xxx CH4:xxx | xxx.x g`
- HX711 离线（读超时）会打印 `ERR: HX711 offline!`。

> **对接泰山派的改造方向**：把 `printf` 输出改为结构化帧（如 `slot,CH1,CH2,CH3,CH4\n`），或直接由泰山派按现有文本格式解析。

---

## 4. 网页控制台（`target_console/index.html`）

单文件、无依赖（Google Fonts 加载失败自动回退本地字体），可直接放到泰山派上静态托管，双击也能本地打开。

### 4.0 视觉风格：赛博朋克 2077

- **配色**：近黑紫底 `#08080e` + 霓虹黄 `#fcee0a` / 故障品红 `#ff2a6d` / 赛博青 `#00f0ff`；阵营色红方 `#ff003c` / 蓝方 `#00f0ff`（通过 CSS 变量 `--acc` 全局联动）
- **字体**：标题 Michroma、正文 Chakra Petch、数据 Share Tech Mono（加载失败自动回退本地字体）
- **氛围**：CRT 扫描线叠加、移动扫描光带、霓虹描边发光、面板切角（clip-path 斜切角）、顶栏底部品红→黄→青渐变霓虹线、标题 glitch 错位重影动画（青/品红偶发闪现）
- **命中视觉**：落点十字为故障品红发光 + 扩散波纹；传感器激活角霓虹黄高亮；通道条青→阵营色渐变，高强度变黄→品红

### 4.1 功能清单

**装甲板监测（左栏，4 块）**
- 官方小装甲板样式 SVG：八边形轮廓（四角 45° 切边）、**左右竖直灯条**（随红蓝阵营变色发光）、中央菱形徽标 + 斜线翼 + 上下箭头印刷纹样
- 每板独立 **ID 输入框**（0~255），改动即下发 `set_id`
- **击打解算**：四角压敏加权质心 → 落点 (X,Y) 显示十字标记 + 扩散波纹，历史落点 8s 渐隐（保留 20 个）
- **击打计数**：单板"清零"按钮 + 顶栏"全部清零"
- 4 通道压力条（命中角亮起，高强度变黄）
- 高通去趋势（慢速基线跟随 `b = b*0.98 + v*0.02`）抑制车体振动；触发阈值 `HIT_THRESHOLD=800`；300ms 去抖防重复计数

**实时模型（右栏 MODEL，俯视/斜视 iOS 分段控件切换）**
- **俯视图**：SVG 平面视角，虚线限轨 + 两端红色限位块；靶车建模（切角底盘、四轮、中心毂 + 航向指针、四块迷你装甲板 N/E/S/W 安装，灯条发光随阵营变色）
- **斜视图（3D）**：CSS `preserve-3d` 真透视（rotateX 58° 斜俯视 + 900px 视距），底盘盒子/四轮/发光轮毂 + 四块竖立装甲板（左右灯条 + 中央菱形徽标）；**装甲板组随旋转独立转动，底盘随移动沿轨平移**，与俯视图共享同一航向角/轨道位置数据
- 虚线限轨 + 两端红色限位块；**到达端点自动停止**并记日志
- `requestAnimationFrame` 仿真循环驱动：旋转 100% ≈ 120RPM（720°/s），轨道 100% 速度 ≈ 12%/s
- 头部实时读数：航向角、轨道位置%

**靶车控制（限轨）**
- 前进 / 停止 / 后退 三档按钮，速度滑块 0~100%，变速即时下发

**旋转控制**
- 开关、顺/逆时针、转速滑块（带 RPM 换算显示 `speed×1.2` RPM）
- 旋转圆盘动画（CSS 关键帧显式 `from 0deg → to 360deg`，逆时针用 `animation-direction:reverse` 实现，修复过"只转 180°"的问题）

**其他**
- 红蓝切换：CSS 变量主题（红 `#ff4d3d` / 蓝 `#2fa8ff`），灯条、纹样、命中标记全部联动
- **iOS 风格控件**：全站 5 组按钮切换（俯视/斜视、前进/停止/后退、顺/逆时针、红蓝方、兵种）统一为 iOS 分段控件——圆角胶囊容器 + `.seg-thumb` 滑块（Apple 弹性曲线 `cubic-bezier(.32,.72,0,1)`，0.42s），滑块颜色随主题变化（红蓝方红/青渐变、兵种随 `--unit-c`）；MutationObserver 监听程序化 `.on` 切换（端点自停、WS 状态回传）保证滑块同步；速度滑块改为白色圆形大滑钮（按下放大 1.12），旋转开关改为 iOS 拨动开关（51×31，白钮投影，开启时主题色发光）
- WebSocket 连接框（默认 `ws://192.168.1.100:8080`），连接成功自动同步全部状态（ID、阵营、移动、旋转）
- **演示模式**：本地模拟随机击打（按四角高斯衰减生成通道值），无硬件即可测试
- 事件日志：命中、命令下发、连接状态等（保留 80 条）

### 4.2 WebSocket 协议（JSON，一行一条）

**前端 → 泰山派（命令）：**

```json
{"cmd":"set_id",     "slot":0, "id":3}                       // 设置第slot块(0~3)装甲板ID
{"cmd":"team",       "value":"red"|"blue"}                     // 红蓝方切换
{"cmd":"reset_hits", "slot":0}                                 // 清零单板；"slot":"all" 全部清零
{"cmd":"move",       "dir":"fwd"|"back"|"stop", "speed":60}    // 限轨移动，speed 0~100
{"cmd":"rotate",     "on":true, "dir":"cw"|"ccw", "speed":45}  // 旋转台开关/方向/速度 0~100
```

**泰山派 → 前端（数据）：**

```json
{"type":"adc","slot":0,"ch":[123,-456,789,-12]}      // 4路HX711原始值（CH1左上/CH2右上/CH3右下/CH4左下）
{"type":"hit","slot":0,"x":42,"y":70,"n":5}          // 可选：板端已解算则直接下发（x,y为0~100百分比，n为累计数）
{"type":"state","rotate":{"on":true,"dir":"cw","speed":45}}  // 可选：状态回传
```

### 4.3 命中解算算法（前端内置）

```
权重  w[i] = max(0, |ch[i]|)                    // 压敏增量取绝对值，截断负噪声
质心  X = Σ(w[i]·x[i]) / Σw[i]  （x[i],y[i] 为四角归一化坐标 0~100）
触发  Σ|去趋势值| > 800 且距上次命中 > 300ms
```

可调常量（JS 顶部）：`SENSOR_POS`（四角坐标）、`HIT_THRESHOLD`（触发阈值）、`MAX_MARKERS`（历史标记数）。

---

## 5. 泰山派 RK3566 侧待办

1. **串口聚合**：4 块 STM32 接 UART/USB 转串口，解析每板的 4 通道数据
2. **WebSocket 服务**：Python `websockets` 库，把 adc/hit 数据广播给前端，接收前端命令转发给 STM32
3. **静态托管**：`index.html` 用简单 HTTP 服务（如 `python3 -m http.server` 或 nginx）对外提供
4. **电机控制**：接收 `move` / `rotate` 命令，驱动轨道电机与旋转电机
5. 可选：把装甲板 ID / 阵营 / 击打数持久化，断电不丢

---

## 6. 踩坑与经验记录

- **双通道 HX711 模块**：传感器接 A 通道，B 通道悬空即可正常工作。
- **JTAG 引脚**：STM32F103 的 PB3/PB4/PA15 默认为 JTAG 复用，引脚分配已刻意避开，无需 remap。
- **HX711 读取超时**：返回 `HX711_ERR_VAL (0x7FFFFFFF)`，主循环检测到任一通道超时会报 `HX711 offline!`，通常是 DT/SCK 接线或共地问题。
- **旋转动画只转 180° 的原因**：CSS 关键帧只写 `to` 不写 `from` 时，动画起点继承元素当前的内联 transform（叶片被设了 `rotate(180deg)`）。修复：关键帧显式 `from{rotate(0deg)} to{rotate(360deg)}`，反向旋转用 `animation-direction:reverse`，不要用内联 transform 表达方向。
- **压敏信号去趋势**：车体振动/静态压偏会造成慢速漂移，前端用一阶慢速基线跟随（系数 0.98/0.02）做高通滤波后再判命中，实践有效。
- **网页动画在后台标签页会暂停**（浏览器节流 rAF），属正常现象，不是 bug。
